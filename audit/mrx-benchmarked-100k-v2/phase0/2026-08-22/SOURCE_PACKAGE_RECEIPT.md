# V2 Phase-0 source package receipt

The complete generated Phase-0 source package contains the full account, repository, branch, CI, PR, release, dependency, maturity, canonical-asset, and security baseline matrices whose SHA-256 values are frozen in `BASELINE_SHA256SUMS.txt` and `BASELINE_MANIFEST.json`.

A local ZIP was created after the frozen source package without modifying the files inside it:

- archive: `DOSSYA_MRX_BENCHMARKED_100K_V2_PHASE0.zip`
- SHA-256: `95a4342fc3b3233bb8c4c8f80960b54c9e8a6e5a309b484666b01f8a6c86a495`

The ZIP is a transport bundle, not an additional scientific result. The individual file hashes in `BASELINE_SHA256SUMS.txt` remain the controlling integrity identities.

The baseline source package was generated before any V2 GitHub write. This repository branch preserves the manifest, receipt, hash list, audit findings, account inventory, timestamp, and this transport receipt. Full matrix bytes remain governed by their frozen SHA-256 values and should be transferred only without byte changes.
